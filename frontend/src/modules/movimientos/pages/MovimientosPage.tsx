import { useState, useMemo } from 'react';import { motion, AnimatePresence } from 'framer-motion';import {  Search,  Plus,  ArrowDownUp,  Eye,  Edit2,  Trash2,  X,  AlertTriangle,  CheckCircle,  Loader2,} from 'lucide-react';import type { Movimiento } from '@/types/index';const initialMovimientos: Movimiento[] = [  { id: '1', tipo: 'entrada', producto: 'Arroz Superior 1kg', codigo: 'ARR-001', cantidad: 500, destino: 'Matriz Quito', motivo: 'Compra a proveedor', usuario: 'María López', fecha: '2026-07-05', sucursal: 'Matriz Quito' },  { id: '2', tipo: 'salida', producto: 'Aceite Vegetal 900ml', codigo: 'ACE-002', cantidad: 200, origen: 'Matriz Quito', motivo: 'Venta al público', usuario: 'Carlos Gómez', fecha: '2026-07-05', sucursal: 'Matriz Quito' },  { id: '3', tipo: 'transferencia', producto: 'Azúcar Morena 2kg', codigo: 'AZU-003', cantidad: 300, origen: 'Matriz Quito', destino: 'Sucursal Norte', motivo: 'Reabastecimiento', usuario: 'Ana Martínez', fecha: '2026-07-04', sucursal: 'Matriz Quito' },  { id: '4', tipo: 'ajuste', producto: 'Leche Entera 1L', codigo: 'LEC-004', cantidad: -15, origen: 'Matriz Quito', motivo: 'Merma por caducidad', usuario: 'Pedro Sánchez', fecha: '2026-07-04', sucursal: 'Matriz Quito' },  { id: '5', tipo: 'entrada', producto: 'Pan Integral 500g', codigo: 'PAN-005', cantidad: 200, destino: 'Sucursal Sur', motivo: 'Producción diaria', usuario: 'José Ruiz', fecha: '2026-07-04', sucursal: 'Sucursal Sur' },  { id: '6', tipo: 'salida', producto: 'Carne de Res 1kg', codigo: 'CAR-006', cantidad: 80, origen: 'Matriz Quito', motivo: 'Venta al público', usuario: 'María López', fecha: '2026-07-03', sucursal: 'Matriz Quito' },  { id: '7', tipo: 'transferencia', producto: 'Detergente Líquido 1L', codigo: 'LIM-010', cantidad: 150, origen: 'Matriz Quito', destino: 'Sucursal Este', motivo: 'Reabastecimiento urgente', usuario: 'Ana Martínez', fecha: '2026-07-03', sucursal: 'Matriz Quito' },  { id: '8', tipo: 'ajuste', producto: 'Huevos (30 unidades)', codigo: 'HUE-008', cantidad: 10, destino: 'Matriz Quito', motivo: 'Ajuste por conteo', usuario: 'Pedro Sánchez', fecha: '2026-07-02', sucursal: 'Matriz Quito' },  { id: '9', tipo: 'entrada', producto: 'Cola Personal 355ml', codigo: 'BEB-009', cantidad: 2000, destino: 'Sucursal Sur', motivo: 'Compra a proveedor', usuario: 'Carlos Gómez', fecha: '2026-07-02', sucursal: 'Sucursal Sur' },  { id: '10', tipo: 'salida', producto: 'Manzana Roja 1kg', codigo: 'FRU-011', cantidad: 300, origen: 'Sucursal Este', motivo: 'Venta al público', usuario: 'José Ruiz', fecha: '2026-07-02', sucursal: 'Sucursal Este' },  { id: '11', tipo: 'transferencia', producto: 'Atún en Lata 180g', codigo: 'CON-015', cantidad: 500, origen: 'Matriz Quito', destino: 'Sucursal Sur', motivo: 'Reabastecimiento', usuario: 'Ana Martínez', fecha: '2026-07-01', sucursal: 'Matriz Quito' },  { id: '12', tipo: 'entrada', producto: 'Queso Fresco 500g', codigo: 'LEC-012', cantidad: 100, destino: 'Sucursal Norte', motivo: 'Compra a proveedor', usuario: 'María López', fecha: '2026-07-01', sucursal: 'Sucursal Norte' },];const tipos = ['entrada', 'salida', 'transferencia', 'ajuste'] as const;const ROWS_PER_PAGE = 10;const tipoBadge = (tipo: string) => {  const map: Record<string, { label: string; class: string }> = {    entrada: { label: 'Entrada', class: 'bg-success/10 text-success border-success/20' },    salida: { label: 'Salida', class: 'bg-error/10 text-error border-error/20' },    transferencia: { label: 'Transferencia', class: 'bg-info/10 text-info border-info/20' },    ajuste: { label: 'Ajuste', class: 'bg-warning/10 text-warning border-warning/20' },  };  return map[tipo] || map.entrada;};export default function MovimientosPage() {  const [search, setSearch] = useState('');  const [tipoFilter, setTipoFilter] = useState('');  const [page, setPage] = useState(1);  const [showModal, setShowModal] = useState(false);  const [editingItem, setEditingItem] = useState<Movimiento | null>(null);  const [showDelete, setShowDelete] = useState<string | null>(null);  const [saving, setSaving] = useState(false);  const [successMsg, setSuccessMsg] = useState('');  const [items, setItems] = useState<Movimiento[]>(initialMovimientos);  const [formData, setFormData] = useState({ tipo: 'entrada' as Movimiento['tipo'], codigo: '', producto: '', cantidad: 0, motivo: '', origen: '', destino: '' });  const filtered = useMemo(() => {    return items.filter((m) => {      if (search && !m.producto.toLowerCase().includes(search.toLowerCase()) && !m.codigo.toLowerCase().includes(search.toLowerCase())) return false;      if (tipoFilter && m.tipo !== tipoFilter) return false;      return true;    });  }, [search, tipoFilter, items]);  const totalPages = Math.ceil(filtered.length / ROWS_PER_PAGE);  const paginated = filtered.slice((page - 1) * ROWS_PER_PAGE, page * ROWS_PER_PAGE);  const handleSave = () => {    setSaving(true);    setTimeout(() => {      if (editingItem) {        setItems(prev => prev.map(p => p.id === editingItem.id ? { ...p, ...formData } : p));      } else {        setItems(prev => [...prev, { id: crypto.randomUUID(), ...formData, usuario: 'Admin', fecha: new Date().toISOString().split('T')[0], sucursal: 'Matriz Quito' }]);      }      setSaving(false);      setShowModal(false);      setEditingItem(null);      setSuccessMsg(editingItem ? 'Movimiento actualizado exitosamente' : 'Movimiento creado exitosamente');      setTimeout(() => setSuccessMsg(''), 3000);    }, 500);  };  const handleDelete = (id: string) => {    setItems(prev => prev.filter(p => p.id !== id));    setShowDelete(null);    setSuccessMsg('Movimiento eliminado exitosamente');    setTimeout(() => setSuccessMsg(''), 3000);  };  const openEdit = (item: Movimiento) => {    setEditingItem(item);    setFormData({ tipo: item.tipo, codigo: item.codigo, producto: item.producto, cantidad: item.cantidad, motivo: item.motivo, origen: item.origen || '', destino: item.destino || '' });    setShowModal(true);  };  const openCreate = () => {    setEditingItem(null);    setFormData({ tipo: 'entrada', codigo: '', producto: '', cantidad: 0, motivo: '', origen: '', destino: '' });    setShowModal(true);  };  return (    <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} className="space-y-5">      <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">        <div>          <h1 className="text-2xl font-bold text-text-primary">Movimientos</h1>          <p className="text-sm text-text-secondary mt-0.5">Historial de movimientos de inventario</p>        </div>        <button onClick={openCreate} className="inline-flex items-center gap-2 px-4 py-2.5 rounded-lg bg-primary-500 text-white text-sm font-semibold hover:bg-primary-600 transition-colors shadow-sm shadow-primary-500/20">          <Plus className="w-4 h-4" />          Nuevo Movimiento        </button>      </div>      <div className="flex flex-col sm:flex-row items-start sm:items-center gap-3">        <div className="relative flex-1 max-w-md">          <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-text-secondary" />          <input            type="text"            value={search}            onChange={(e) => { setSearch(e.target.value); setPage(1); }}            placeholder="Buscar por producto o código..."            className="w-full pl-9 pr-4 py-2.5 rounded-lg border border-border bg-white text-sm text-text-primary placeholder:text-text-secondary/40 outline-none focus:ring-2 focus:ring-primary-500/20 focus:border-primary-500 transition-all"          />        </div>        <div className="flex items-center gap-2">          <select value={tipoFilter} onChange={(e) => { setTipoFilter(e.target.value); setPage(1); }} className="px-3 py-2.5 rounded-lg border border-border bg-white text-sm text-text-primary outline-none focus:ring-2 focus:ring-primary-500/20 focus:border-primary-500 transition-all">            <option value="">Todos los tipos</option>            {tipos.map((t) => <option key={t} value={t}>{t.charAt(0).toUpperCase() + t.slice(1)}</option>)}          </select>        </div>      </div>      <div className="bg-white rounded-xl border border-border overflow-hidden">        <div className="overflow-x-auto">          <table className="w-full">            <thead>              <tr className="border-b border-border bg-gray-50/50">                <th className="text-left px-4 py-3.5 text-xs font-semibold text-text-secondary uppercase tracking-wider">Tipo</th>                <th className="text-left px-4 py-3.5 text-xs font-semibold text-text-secondary uppercase tracking-wider">Producto</th>                <th className="text-right px-4 py-3.5 text-xs font-semibold text-text-secondary uppercase tracking-wider">Cantidad</th>                <th className="text-left px-4 py-3.5 text-xs font-semibold text-text-secondary uppercase tracking-wider">Origen/Destino</th>                <th className="text-left px-4 py-3.5 text-xs font-semibold text-text-secondary uppercase tracking-wider">Motivo</th>                <th className="text-left px-4 py-3.5 text-xs font-semibold text-text-secondary uppercase tracking-wider">Usuario</th>                <th className="text-left px-4 py-3.5 text-xs font-semibold text-text-secondary uppercase tracking-wider">Fecha</th>                <th className="text-center px-4 py-3.5 text-xs font-semibold text-text-secondary uppercase tracking-wider">Acciones</th>              </tr>            </thead>            <tbody className="divide-y divide-border">              {paginated.map((item) => (                <motion.tr                  key={item.id}                  initial={{ opacity: 0 }}                  animate={{ opacity: 1 }}                  className="hover:bg-gray-50/50 transition-colors"                >                  <td className="px-4 py-3.5">                    <span className={`inline-flex items-center px-2 py-0.5 rounded-full text-[11px] font-medium border ${tipoBadge(item.tipo).class}`}>                      {tipoBadge(item.tipo).label}                    </span>                  </td>                  <td className="px-4 py-3.5">                    <div className="flex items-center gap-3">                      <div className="w-8 h-8 rounded-lg bg-primary-50 flex items-center justify-center">                        <ArrowDownUp className="w-4 h-4 text-primary-500" />                      </div>                      <div>                        <p className="text-sm font-medium text-text-primary">{item.producto}</p>                        <p className="text-xs text-text-secondary">{item.codigo}</p>                      </div>                    </div>                  </td>                  <td className="px-4 py-3.5 text-right">                    <span className={`text-sm font-semibold ${item.cantidad < 0 ? 'text-error' : 'text-success'}`}>                      {item.cantidad > 0 ? '+' : ''}{item.cantidad}                    </span>                  </td>                  <td className="px-4 py-3.5 text-sm text-text-secondary">{item.origen || item.destino || '-'}</td>                  <td className="px-4 py-3.5 text-sm text-text-secondary max-w-[200px] truncate">{item.motivo}</td>                  <td className="px-4 py-3.5 text-sm text-text-secondary">{item.usuario}</td>                  <td className="px-4 py-3.5 text-sm text-text-secondary">{item.fecha}</td>                  <td className="px-4 py-3.5">                    <div className="flex items-center justify-center gap-1">                      <button className="p-1.5 rounded-lg text-text-secondary hover:text-primary-500 hover:bg-primary-50 transition-all" title="Ver">                        <Eye className="w-4 h-4" />                      </button>                      <button onClick={() => openEdit(item)} className="p-1.5 rounded-lg text-text-secondary hover:text-primary-500 hover:bg-primary-50 transition-all" title="Editar">                        <Edit2 className="w-4 h-4" />                      </button>                      <button onClick={() => setShowDelete(item.id)} className="p-1.5 rounded-lg text-text-secondary hover:text-error hover:bg-error/10 transition-all" title="Eliminar">                        <Trash2 className="w-4 h-4" />                      </button>                    </div>                  </td>                </motion.tr>              ))}              {paginated.length === 0 && (                <tr>                  <td colSpan={8} className="px-4 py-12 text-center text-text-secondary">                    <ArrowDownUp className="w-10 h-10 mx-auto mb-2 opacity-30" />                    <p className="text-sm font-medium">No se encontraron movimientos</p>                    <p className="text-xs mt-0.5">Intenta ajustar los filtros o crea un nuevo movimiento</p>                  </td>                </tr>              )}            </tbody>          </table>        </div>        {totalPages > 1 && (          <div className="flex items-center justify-between px-4 py-3 border-t border-border">            <p className="text-sm text-text-secondary">              Mostrando {(page - 1) * ROWS_PER_PAGE + 1}-{Math.min(page * ROWS_PER_PAGE, filtered.length)} de {filtered.length}            </p>            <div className="flex items-center gap-1">              <button disabled={page <= 1} onClick={() => setPage(page - 1)} className="px-3 py-1.5 rounded-lg text-sm font-medium text-text-secondary hover:bg-gray-100 disabled:opacity-30 disabled:cursor-not-allowed transition-all">                Anterior              </button>              {Array.from({ length: Math.min(totalPages, 5) }, (_, i) => {                const start = Math.max(1, Math.min(page - 2, totalPages - 4));                const p = start + i;                if (p > totalPages) return null;                return (                  <button key={p} onClick={() => setPage(p)} className={`w-8 h-8 rounded-lg text-sm font-medium transition-all ${page === p ? 'bg-primary-500 text-white' : 'text-text-secondary hover:bg-gray-100'}`}>                    {p}                  </button>                );              })}              <button disabled={page >= totalPages} onClick={() => setPage(page + 1)} className="px-3 py-1.5 rounded-lg text-sm font-medium text-text-secondary hover:bg-gray-100 disabled:opacity-30 disabled:cursor-not-allowed transition-all">                Siguiente              </button>            </div>          </div>        )}      </div>      <AnimatePresence>        {showModal && (          <motion.div key="modal" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 z-50 flex items-center justify-center p-4">            <div className="absolute inset-0 bg-black/40" onClick={() => setShowModal(false)} />            <motion.div initial={{ scale: 0.95, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} exit={{ scale: 0.95, opacity: 0 }} className="relative bg-white rounded-2xl shadow-xl w-full max-w-lg max-h-[90vh] overflow-y-auto">              <div className="flex items-center justify-between px-6 py-4 border-b border-border">                <h3 className="text-lg font-semibold text-text-primary">{editingItem ? 'Editar Movimiento' : 'Nuevo Movimiento'}</h3>                <button onClick={() => setShowModal(false)} className="p-1.5 rounded-lg text-text-secondary hover:bg-gray-100 transition-colors">                  <X className="w-5 h-5" />                </button>              </div>              <div className="p-6 space-y-4">                <div className="grid grid-cols-2 gap-4">                  <div>                    <label className="block text-sm font-medium text-text-primary mb-1.5">Tipo</label>                    <select value={formData.tipo} onChange={(e) => setFormData({...formData, tipo: e.target.value as Movimiento['tipo']})} className="w-full px-3 py-2.5 rounded-lg border border-border bg-white text-sm text-text-primary outline-none focus:ring-2 focus:ring-primary-500/20 focus:border-primary-500 transition-all">                      <option value="entrada">Entrada</option>                      <option value="salida">Salida</option>                      <option value="transferencia">Transferencia</option>                      <option value="ajuste">Ajuste</option>                    </select>                  </div>                  <div>                    <label className="block text-sm font-medium text-text-primary mb-1.5">Código</label>                    <input value={formData.codigo} onChange={(e) => setFormData({...formData, codigo: e.target.value})} className="w-full px-3 py-2.5 rounded-lg border border-border bg-white text-sm text-text-primary outline-none focus:ring-2 focus:ring-primary-500/20 focus:border-primary-500 transition-all" placeholder="Ej: ABC-001" />                  </div>                </div>                <div>                  <label className="block text-sm font-medium text-text-primary mb-1.5">Producto</label>                  <input value={formData.producto} onChange={(e) => setFormData({...formData, producto: e.target.value})} className="w-full px-3 py-2.5 rounded-lg border border-border bg-white text-sm text-text-primary outline-none focus:ring-2 focus:ring-primary-500/20 focus:border-primary-500 transition-all" placeholder="Nombre del producto" />                </div>                <div className="grid grid-cols-2 gap-4">                  <div>                    <label className="block text-sm font-medium text-text-primary mb-1.5">Cantidad</label>                    <input type="number" value={formData.cantidad} onChange={(e) => setFormData({...formData, cantidad: Number(e.target.value)})} className="w-full px-3 py-2.5 rounded-lg border border-border bg-white text-sm text-text-primary outline-none focus:ring-2 focus:ring-primary-500/20 focus:border-primary-500 transition-all" placeholder="0" />                  </div>                  <div>                    <label className="block text-sm font-medium text-text-primary mb-1.5">Motivo</label>                    <input value={formData.motivo} onChange={(e) => setFormData({...formData, motivo: e.target.value})} className="w-full px-3 py-2.5 rounded-lg border border-border bg-white text-sm text-text-primary outline-none focus:ring-2 focus:ring-primary-500/20 focus:border-primary-500 transition-all" placeholder="Motivo del movimiento" />                  </div>                </div>                <div className="grid grid-cols-2 gap-4">                  <div>                    <label className="block text-sm font-medium text-text-primary mb-1.5">Origen</label>                    <input value={formData.origen} onChange={(e) => setFormData({...formData, origen: e.target.value})} className="w-full px-3 py-2.5 rounded-lg border border-border bg-white text-sm text-text-primary outline-none focus:ring-2 focus:ring-primary-500/20 focus:border-primary-500 transition-all" placeholder="Origen" />                  </div>                  <div>                    <label className="block text-sm font-medium text-text-primary mb-1.5">Destino</label>                    <input value={formData.destino} onChange={(e) => setFormData({...formData, destino: e.target.value})} className="w-full px-3 py-2.5 rounded-lg border border-border bg-white text-sm text-text-primary outline-none focus:ring-2 focus:ring-primary-500/20 focus:border-primary-500 transition-all" placeholder="Destino" />                  </div>                </div>              </div>              <div className="flex items-center justify-end gap-3 px-6 py-4 border-t border-border bg-gray-50/50">                <button onClick={() => setShowModal(false)} className="px-4 py-2.5 rounded-lg border border-border text-sm font-medium text-text-secondary hover:bg-gray-100 transition-colors">                  Cancelar                </button>                <button onClick={handleSave} disabled={saving} className="px-4 py-2.5 rounded-lg bg-primary-500 text-white text-sm font-semibold hover:bg-primary-600 disabled:opacity-60 disabled:cursor-not-allowed transition-all flex items-center gap-2">                  {saving && <Loader2 className="w-4 h-4 animate-spin" />}                  {saving ? 'Guardando...' : editingItem ? 'Actualizar Movimiento' : 'Crear Movimiento'}                </button>              </div>            </motion.div>          </motion.div>        )}      </AnimatePresence>      <AnimatePresence>        {showDelete && (          <motion.div key="delete" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 z-50 flex items-center justify-center p-4">            <div className="absolute inset-0 bg-black/40" onClick={() => setShowDelete(null)} />            <motion.div initial={{ scale: 0.95 }} animate={{ scale: 1 }} exit={{ scale: 0.95 }} className="relative bg-white rounded-2xl shadow-xl w-full max-w-sm p-6 text-center">              <div className="w-12 h-12 rounded-full bg-error/10 flex items-center justify-center mx-auto mb-4">                <AlertTriangle className="w-6 h-6 text-error" />              </div>              <h3 className="text-lg font-semibold text-text-primary mb-2">¿Eliminar movimiento?</h3>              <p className="text-sm text-text-secondary mb-6">Esta acción no se puede deshacer. El movimiento será eliminado permanentemente.</p>              <div className="flex items-center justify-center gap-3">                <button onClick={() => setShowDelete(null)} className="px-4 py-2.5 rounded-lg border border-border text-sm font-medium text-text-secondary hover:bg-gray-100 transition-colors">                  Cancelar                </button>                <button onClick={() => handleDelete(showDelete)} className="px-4 py-2.5 rounded-lg bg-error text-white text-sm font-semibold hover:bg-red-700 transition-colors flex items-center gap-2">                  <Trash2 className="w-4 h-4" />                  Eliminar                </button>              </div>            </motion.div>          </motion.div>        )}      </AnimatePresence>      <AnimatePresence>        {successMsg && (          <motion.div key="toast" initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} className="fixed bottom-6 right-6 z-50 flex items-center gap-2 px-4 py-3 rounded-lg bg-success text-white text-sm font-medium shadow-lg">            <CheckCircle className="w-4 h-4" />            {successMsg}          </motion.div>        )}      </AnimatePresence>    </motion.div>  );}