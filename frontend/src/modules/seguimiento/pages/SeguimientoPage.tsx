import { useState, useMemo } from 'react';import { motion, AnimatePresence } from 'framer-motion';import { Search, Plus, Eye, Edit2, Trash2, X, CheckCircle, AlertTriangle, Loader2 } from 'lucide-react';import type { SeguimientoCliente } from '@/types/index';const initialSeguimiento: SeguimientoCliente[] = [  { id: '1', cliente: 'María Fernanda Gómez', tipo: 'llamada', fecha: '2026-07-05', motivo: 'Seguimiento post-compra', resultado: 'Cliente satisfecha', usuario: 'Carlos Rivera', proximoContacto: '2026-07-20' },  { id: '2', cliente: 'Comercializadora Quito SA', tipo: 'correo', fecha: '2026-07-04', motivo: 'Cotización de productos', resultado: 'Enviada cotización solicitada', usuario: 'Ana Paz', proximoContacto: '2026-07-11' },  { id: '3', cliente: 'Carlos Andrés López', tipo: 'visita', fecha: '2026-07-03', motivo: 'Presentación de nuevos productos', resultado: 'Interesado en comprar', usuario: 'Carlos Rivera', proximoContacto: '2026-07-10' },  { id: '4', cliente: 'Supermercados del Pacífico SA', tipo: 'whatsapp', fecha: '2026-07-02', motivo: 'Confirmación de pedido', resultado: 'Pedido confirmado', usuario: 'María Torres', proximoContacto: '2026-07-09' },  { id: '5', cliente: 'Ana Belén Martínez', tipo: 'llamada', fecha: '2026-07-01', motivo: 'Encuesta de satisfacción', resultado: 'Calificación 9/10', usuario: 'Ana Paz', proximoContacto: '2026-07-15' },  { id: '6', cliente: 'Distribuidora Andina Cía. Ltda.', tipo: 'correo', fecha: '2026-06-30', motivo: 'Oferta especial mayo', resultado: 'Sin respuesta', usuario: 'Carlos Rivera', proximoContacto: '2026-07-07' },  { id: '7', cliente: 'Luisa Fernanda Torres', tipo: 'visita', fecha: '2026-06-28', motivo: 'Renovación de membresía', resultado: 'Membresía renovada por 1 año', usuario: 'María Torres', proximoContacto: '2027-06-28' },  { id: '8', cliente: 'Mercagran S.A.', tipo: 'whatsapp', fecha: '2026-06-27', motivo: 'Consulta de precios', resultado: 'Precios enviados', usuario: 'Ana Paz', proximoContacto: '2026-07-04' },  { id: '9', cliente: 'Almacenes Cuenca Cía. Ltda.', tipo: 'llamada', fecha: '2026-06-25', motivo: 'Solución de reclamo', resultado: 'Reclamo solucionado', usuario: 'Carlos Rivera', proximoContacto: '2026-07-10' },  { id: '10', cliente: 'Gabriela Alexandra Ruiz', tipo: 'correo', fecha: '2026-06-24', motivo: 'Notificación de factura', resultado: 'Factura enviada', usuario: 'María Torres', proximoContacto: '2026-07-24' },  { id: '11', cliente: 'Farmacias Saludable Cía. Ltda.', tipo: 'visita', fecha: '2026-06-22', motivo: 'Negociación de contrato', resultado: 'Contrato firmado', usuario: 'Ana Paz', proximoContacto: '2026-08-22' },  { id: '12', cliente: 'Carmen Elena Vargas', tipo: 'whatsapp', fecha: '2026-06-20', motivo: 'Recordatorio de pago', resultado: 'Pago realizado', usuario: 'Carlos Rivera', proximoContacto: '2026-07-20' },];const tipos = [...new Set(initialSeguimiento.map(s => s.tipo))];const usuarios = [...new Set(initialSeguimiento.map(s => s.usuario))];const typeBadge: Record<string, string> = {  llamada: 'bg-blue-50 text-info',  correo: 'bg-blue-50 text-info',  visita: 'bg-success-lighter text-success',  whatsapp: 'bg-success-lighter text-success',};const ITEMS_PER_PAGE = 8;function SeguimientoPage() {  const [search, setSearch] = useState('');  const [filtroTipo, setFiltroTipo] = useState<string>('');  const [filtroUsuario, setFiltroUsuario] = useState<string>('');  const [currentPage, setCurrentPage] = useState(1);  const [modalOpen, setModalOpen] = useState(false);  const [editando, setEditando] = useState<SeguimientoCliente | null>(null);  const [deleteOpen, setDeleteOpen] = useState(false);  const [toast, setToast] = useState<{ visible: boolean; message: string }>({ visible: false, message: '' });  const [saving, setSaving] = useState(false);  const [form, setForm] = useState<Partial<SeguimientoCliente>>({});  const [items, setItems] = useState<SeguimientoCliente[]>(initialSeguimiento);  const [deleteTarget, setDeleteTarget] = useState<string | null>(null);  const filtrados = useMemo(() => {    return items.filter(s => {      const matchSearch = !search || s.cliente.toLowerCase().includes(search.toLowerCase()) || s.motivo.toLowerCase().includes(search.toLowerCase());      const matchTipo = !filtroTipo || s.tipo === filtroTipo;      const matchUsuario = !filtroUsuario || s.usuario === filtroUsuario;      return matchSearch && matchTipo && matchUsuario;    });  }, [search, filtroTipo, filtroUsuario, items]);  const totalPages = Math.ceil(filtrados.length / ITEMS_PER_PAGE);  const paginados = useMemo(() => {    const start = (currentPage - 1) * ITEMS_PER_PAGE;    return filtrados.slice(start, start + ITEMS_PER_PAGE);  }, [filtrados, currentPage]);  const showToast = (message: string) => {    setToast({ visible: true, message });    setTimeout(() => setToast({ visible: false, message: '' }), 3000);  };  const abrirNuevo = () => {    setEditando(null);    setForm({ tipo: 'llamada' });    setModalOpen(true);  };  const abrirEditar = (s: SeguimientoCliente) => {    setEditando(s);    setForm({ ...s });    setModalOpen(true);  };  const guardar = () => {    setSaving(true);    setTimeout(() => {      if (editando) {        setItems(prev => prev.map(p => p.id === editando.id ? { ...p, ...form } : p));      } else {        setItems(prev => [...prev, { ...form, id: crypto.randomUUID() } as SeguimientoCliente]);      }      setSaving(false);      setModalOpen(false);      showToast(editando ? 'Seguimiento actualizado exitosamente' : 'Seguimiento creado exitosamente');    }, 1000);  };  const confirmarEliminar = (id: string) => {    setDeleteTarget(id);    setDeleteOpen(true);  };  const eliminar = () => {    if (!deleteTarget) return;    setItems(prev => prev.filter(p => p.id !== deleteTarget));    setDeleteTarget(null);    setDeleteOpen(false);    showToast('Seguimiento eliminado exitosamente');  };  const handleFormChange = (field: keyof SeguimientoCliente, value: string) => {    setForm(prev => ({ ...prev, [field]: value }));  };  return (    <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }}>        <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className="mb-8">          <h1 className="text-2xl font-bold text-text-primary">Seguimiento de Clientes</h1>          <p className="text-text-secondary mt-1">Registro de interacciones con clientes</p>        </motion.div>        <div className="bg-white rounded-xl border border-border p-5 mb-6">          <div className="flex flex-wrap items-center gap-4">            <div className="relative flex-1 min-w-[240px]">              <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-text-secondary" />              <input                type="text"                placeholder="Buscar seguimiento..."                value={search}                onChange={e => { setSearch(e.target.value); setCurrentPage(1); }}                className="w-full rounded-lg border border-border bg-white pl-10 pr-3 py-2.5 text-sm text-text-primary placeholder:text-text-secondary focus:outline-none focus:ring-2 focus:ring-primary-500"              />            </div>            <select              value={filtroTipo}              onChange={e => { setFiltroTipo(e.target.value); setCurrentPage(1); }}              className="rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-secondary focus:outline-none focus:ring-2 focus:ring-primary-500"            >              <option value="">Todos los tipos</option>              {tipos.map(t => <option key={t} value={t}>{t.charAt(0).toUpperCase() + t.slice(1)}</option>)}            </select>            <select              value={filtroUsuario}              onChange={e => { setFiltroUsuario(e.target.value); setCurrentPage(1); }}              className="rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-secondary focus:outline-none focus:ring-2 focus:ring-primary-500"            >              <option value="">Todos los usuarios</option>              {usuarios.map(u => <option key={u} value={u}>{u}</option>)}            </select>            <button              onClick={abrirNuevo}              className="inline-flex items-center gap-2 bg-primary-500 text-white px-4 py-2.5 rounded-lg text-sm font-medium hover:bg-primary-600 transition-colors"            >              <Plus className="w-4 h-4" />              Nuevo            </button>          </div>        </div>        <div className="bg-white rounded-xl border border-border overflow-hidden">          {filtrados.length === 0 ? (            <div className="flex flex-col items-center justify-center py-16 px-4">              <div className="w-16 h-16 rounded-full bg-primary-50 flex items-center justify-center mb-4">                <Search className="w-8 h-8 text-primary-500" />              </div>              <h3 className="text-lg font-semibold text-text-primary mb-1">No se encontraron registros</h3>              <p className="text-sm text-text-secondary text-center max-w-sm">Intenta ajustar los filtros o términos de búsqueda</p>            </div>          ) : (            <div className="overflow-x-auto">              <table className="w-full">                <thead>                  <tr className="border-b border-border bg-gray-50">                    <th className="text-left px-4 py-3 text-xs font-semibold text-text-secondary uppercase tracking-wider">Cliente</th>                    <th className="text-center px-4 py-3 text-xs font-semibold text-text-secondary uppercase tracking-wider">Tipo</th>                    <th className="text-left px-4 py-3 text-xs font-semibold text-text-secondary uppercase tracking-wider">Fecha</th>                    <th className="text-left px-4 py-3 text-xs font-semibold text-text-secondary uppercase tracking-wider">Motivo</th>                    <th className="text-left px-4 py-3 text-xs font-semibold text-text-secondary uppercase tracking-wider">Resultado</th>                    <th className="text-left px-4 py-3 text-xs font-semibold text-text-secondary uppercase tracking-wider">Usuario</th>                    <th className="text-left px-4 py-3 text-xs font-semibold text-text-secondary uppercase tracking-wider">Próximo Contacto</th>                    <th className="text-right px-4 py-3 text-xs font-semibold text-text-secondary uppercase tracking-wider">Acciones</th>                  </tr>                </thead>                <tbody className="divide-y divide-border">                  {paginados.map(s => (                    <tr key={s.id} className="hover:bg-gray-50 transition-colors">                      <td className="px-4 py-3 text-sm text-text-primary font-medium">{s.cliente}</td>                      <td className="px-4 py-3 text-center">                        <span className={`inline-flex items-center rounded-full px-2.5 py-1 text-xs font-medium ${typeBadge[s.tipo] || 'bg-gray-100 text-text-secondary'}`}>                          {s.tipo.charAt(0).toUpperCase() + s.tipo.slice(1)}                        </span>                      </td>                      <td className="px-4 py-3 text-sm text-text-secondary">{s.fecha}</td>                      <td className="px-4 py-3 text-sm text-text-secondary max-w-[200px] truncate">{s.motivo}</td>                      <td className="px-4 py-3 text-sm text-text-secondary max-w-[200px] truncate">{s.resultado}</td>                      <td className="px-4 py-3 text-sm text-text-secondary">{s.usuario}</td>                      <td className="px-4 py-3 text-sm text-text-secondary">{s.proximoContacto}</td>                      <td className="px-4 py-3 text-right">                        <div className="flex items-center justify-end gap-1">                          <button className="p-2 rounded-lg text-text-secondary hover:bg-gray-100 hover:text-primary-500 transition-colors" title="Ver"><Eye className="w-4 h-4" /></button>                          <button onClick={() => abrirEditar(s)} className="p-2 rounded-lg text-text-secondary hover:bg-gray-100 hover:text-primary-500 transition-colors" title="Editar"><Edit2 className="w-4 h-4" /></button>                          <button onClick={() => confirmarEliminar(s.id)} className="p-2 rounded-lg text-text-secondary hover:bg-gray-100 hover:text-error transition-colors" title="Eliminar"><Trash2 className="w-4 h-4" /></button>                        </div>                      </td>                    </tr>                  ))}                </tbody>              </table>            </div>          )}          {filtrados.length > 0 && (            <div className="flex items-center justify-between px-4 py-3 border-t border-border">              <span className="text-sm text-text-secondary">                Mostrando {Math.min((currentPage - 1) * ITEMS_PER_PAGE + 1, filtrados.length)}-{Math.min(currentPage * ITEMS_PER_PAGE, filtrados.length)} de {filtrados.length}              </span>              <div className="flex items-center gap-1">                {Array.from({ length: totalPages }, (_, i) => i + 1).map(page => (                  <button                    key={page}                    onClick={() => setCurrentPage(page)}                    className={`inline-flex items-center justify-center min-w-[36px] h-9 px-2 rounded-lg border text-sm font-medium transition-colors ${page === currentPage ? 'bg-primary-500 text-white border-primary-500' : 'border-border text-text-secondary hover:bg-gray-100'}`}                  >                    {page}                  </button>                ))}              </div>            </div>          )}        </div>      <AnimatePresence>        {modalOpen && (          <motion.div key="modal" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 z-50 flex items-center justify-center p-4">            <div className="absolute inset-0 bg-black/40 backdrop-blur-sm" onClick={() => setModalOpen(false)} />            <motion.div initial={{ opacity: 0, scale: 0.95 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0, scale: 0.95 }} className="relative w-full max-w-lg bg-white rounded-xl shadow-xl">              <div className="flex items-center justify-between px-6 pt-6 pb-0">                <h2 className="text-lg font-semibold text-text-primary">{editando ? 'Editar Seguimiento' : 'Nuevo Seguimiento'}</h2>                <button onClick={() => setModalOpen(false)} className="p-1 rounded-lg text-text-secondary hover:bg-gray-100 transition-colors"><X className="w-5 h-5" /></button>              </div>              <div className="px-6 py-6 space-y-4">                <div>                  <label className="block text-sm font-medium text-text-primary mb-1">Cliente</label>                  <input type="text" value={form.cliente || ''} onChange={e => handleFormChange('cliente', e.target.value)} className="w-full rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-primary-500" />                </div>                <div>                  <label className="block text-sm font-medium text-text-primary mb-1">Tipo</label>                  <select value={form.tipo || 'llamada'} onChange={e => handleFormChange('tipo', e.target.value)} className="w-full rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-primary-500">                    <option value="llamada">Llamada</option>                    <option value="correo">Correo</option>                    <option value="visita">Visita</option>                    <option value="whatsapp">WhatsApp</option>                  </select>                </div>                <div className="grid grid-cols-2 gap-4">                  <div>                    <label className="block text-sm font-medium text-text-primary mb-1">Fecha</label>                    <input type="date" value={form.fecha || ''} onChange={e => handleFormChange('fecha', e.target.value)} className="w-full rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-primary-500" />                  </div>                  <div>                    <label className="block text-sm font-medium text-text-primary mb-1">Usuario</label>                    <input type="text" value={form.usuario || ''} onChange={e => handleFormChange('usuario', e.target.value)} className="w-full rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-primary-500" />                  </div>                </div>                <div>                  <label className="block text-sm font-medium text-text-primary mb-1">Motivo</label>                  <input type="text" value={form.motivo || ''} onChange={e => handleFormChange('motivo', e.target.value)} className="w-full rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-primary-500" />                </div>                <div>                  <label className="block text-sm font-medium text-text-primary mb-1">Resultado</label>                  <input type="text" value={form.resultado || ''} onChange={e => handleFormChange('resultado', e.target.value)} className="w-full rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-primary-500" />                </div>                <div>                  <label className="block text-sm font-medium text-text-primary mb-1">Próximo Contacto</label>                  <input type="date" value={form.proximoContacto || ''} onChange={e => handleFormChange('proximoContacto', e.target.value)} className="w-full rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-primary-500" />                </div>              </div>              <div className="px-6 pb-6 flex items-center justify-end gap-3">                <button onClick={() => setModalOpen(false)} className="px-4 py-2 rounded-lg border border-border text-text-secondary text-sm font-medium hover:bg-gray-100 transition-colors">Cancelar</button>                <button onClick={guardar} disabled={saving} className="inline-flex items-center gap-2 bg-primary-500 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-primary-600 transition-colors disabled:opacity-50">                  {saving && <Loader2 className="w-4 h-4 animate-spin" />}                  {saving ? 'Guardando...' : 'Guardar'}                </button>              </div>            </motion.div>          </motion.div>        )}      </AnimatePresence>      <AnimatePresence>        {deleteOpen && (          <motion.div key="delete" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 z-50 flex items-center justify-center p-4">            <div className="absolute inset-0 bg-black/40 backdrop-blur-sm" onClick={() => setDeleteOpen(false)} />            <motion.div initial={{ opacity: 0, scale: 0.95 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0, scale: 0.95 }} className="relative w-full max-w-sm bg-white rounded-xl shadow-xl">              <div className="px-6 pt-6 pb-4 text-center">                <div className="w-12 h-12 rounded-full bg-error-lighter flex items-center justify-center mx-auto mb-4">                  <AlertTriangle className="w-6 h-6 text-error" />                </div>                <h2 className="text-lg font-semibold text-text-primary mb-2">Confirmar eliminación</h2>                <p className="text-sm text-text-secondary">¿Estás seguro de eliminar este registro? Esta acción no se puede deshacer.</p>              </div>              <div className="px-6 pb-6 flex items-center justify-center gap-3">                <button onClick={() => setDeleteOpen(false)} className="px-4 py-2 rounded-lg border border-border text-text-secondary text-sm font-medium hover:bg-gray-100 transition-colors">Cancelar</button>                <button onClick={eliminar} className="px-4 py-2 rounded-lg bg-error text-white text-sm font-medium hover:bg-red-700 transition-colors">Eliminar</button>              </div>            </motion.div>          </motion.div>        )}      </AnimatePresence>      <AnimatePresence>        {toast.visible && (          <motion.div key="toast"            initial={{ opacity: 0, x: 100, scale: 0.95 }}            animate={{ opacity: 1, x: 0, scale: 1 }}            exit={{ opacity: 0, x: 100, scale: 0.95 }}            className="fixed bottom-6 right-6 z-50 max-w-sm w-full bg-white rounded-lg shadow-lg border border-border border-l-4 border-l-success px-4 py-3"          >            <div className="flex items-start gap-3">              <CheckCircle className="w-5 h-5 mt-0.5 flex-shrink-0 text-success" />              <p className="flex-1 text-sm text-text-primary">{toast.message}</p>              <button onClick={() => setToast({ visible: false, message: '' })} className="p-1 rounded-lg text-text-secondary hover:text-text-primary hover:bg-gray-100 transition-colors flex-shrink-0"><X className="w-4 h-4" /></button>            </div>          </motion.div>        )}      </AnimatePresence>    </motion.div>  );}export default SeguimientoPage;