import { useState, useMemo } from 'react';import { motion, AnimatePresence } from 'framer-motion';import { Search, Plus, Eye, Edit2, Trash2, X, CheckCircle, AlertTriangle, Loader2, MessageSquare, Clock, CheckSquare, Star } from 'lucide-react';import type { Ticket } from '@/types/index';const initialTickets: Ticket[] = [  { id: '1', numero: 'TK-001', cliente: 'María Fernanda Gómez', asunto: 'Producto caducado en compra', categoria: 'calidad', prioridad: 'alta', fecha: '2026-07-05', usuario: 'Pedro Pérez', estado: 'abierto', ultimaActualizacion: '2026-07-05' },  { id: '2', numero: 'TK-002', cliente: 'Comercializadora Quito SA', asunto: 'Error en facturación', categoria: 'facturacion', prioridad: 'critica', fecha: '2026-07-04', usuario: 'Lucía Medina', estado: 'en_proceso', ultimaActualizacion: '2026-07-06' },  { id: '3', numero: 'TK-003', cliente: 'Carlos Andrés López', asunto: 'Consulta sobre devolución', categoria: 'devoluciones', prioridad: 'baja', fecha: '2026-07-03', usuario: 'Pedro Pérez', estado: 'resuelto', ultimaActualizacion: '2026-07-04' },  { id: '4', numero: 'TK-004', cliente: 'Supermercados del Pacífico SA', asunto: 'Solicitud de cotización mayorista', categoria: 'ventas', prioridad: 'media', fecha: '2026-07-02', usuario: 'Ana Torres', estado: 'pendiente', ultimaActualizacion: '2026-07-03' },  { id: '5', numero: 'TK-005', cliente: 'Ana Belén Martínez', asunto: 'Problema con entrega a domicilio', categoria: 'logistica', prioridad: 'alta', fecha: '2026-07-01', usuario: 'Lucía Medina', estado: 'abierto', ultimaActualizacion: '2026-07-02' },  { id: '6', numero: 'TK-006', cliente: 'Distribuidora Andina Cía. Ltda.', asunto: 'Reclamo por cobro duplicado', categoria: 'facturacion', prioridad: 'critica', fecha: '2026-06-30', usuario: 'Pedro Pérez', estado: 'en_proceso', ultimaActualizacion: '2026-07-01' },  { id: '7', numero: 'TK-007', cliente: 'Luisa Fernanda Torres', asunto: 'Solicitud de factura electrónica', categoria: 'facturacion', prioridad: 'baja', fecha: '2026-06-28', usuario: 'Ana Torres', estado: 'cerrado', ultimaActualizacion: '2026-06-29' },  { id: '8', numero: 'TK-008', cliente: 'Mercagran S.A.', asunto: 'Actualización de datos de empresa', categoria: 'datos', prioridad: 'media', fecha: '2026-06-27', usuario: 'Lucía Medina', estado: 'resuelto', ultimaActualizacion: '2026-06-28' },];const prioridadBadge: Record<string, string> = {  baja: 'bg-gray-100 text-text-secondary',  media: 'bg-blue-50 text-info',  alta: 'bg-amber-50 text-warning',  critica: 'bg-red-50 text-error',};const estadoBadge: Record<string, string> = {  abierto: 'bg-blue-50 text-info',  en_proceso: 'bg-amber-50 text-warning',  pendiente: 'bg-amber-100 text-amber-800',  resuelto: 'bg-success-lighter text-success',  cerrado: 'bg-gray-100 text-text-secondary',};const summaryCards = [  { title: 'Tickets Abiertos', value: 3, icon: MessageSquare, color: 'text-info' },  { title: 'En Proceso', value: 2, icon: Clock, color: 'text-warning' },  { title: 'Resueltos', value: 2, icon: CheckSquare, color: 'text-success' },  { title: 'Satisfacción Promedio', value: '4.5 / 5.0', icon: Star, color: 'text-yellow-500' },];function AtencionClientePage() {  const [search, setSearch] = useState('');  const [filtroEstado, setFiltroEstado] = useState<string>('');  const [filtroPrioridad, setFiltroPrioridad] = useState<string>('');  const [currentPage, setCurrentPage] = useState(1);  const [modalOpen, setModalOpen] = useState(false);  const [editando, setEditando] = useState<Ticket | null>(null);  const [deleteOpen, setDeleteOpen] = useState(false);  const [toast, setToast] = useState<{ visible: boolean; message: string }>({ visible: false, message: '' });  const [saving, setSaving] = useState(false);  const [form, setForm] = useState<Partial<Ticket>>({});  const [items, setItems] = useState<Ticket[]>(initialTickets);  const [deleteTarget, setDeleteTarget] = useState<string | null>(null);  const filtrados = useMemo(() => {    return items.filter(t => {      const matchSearch = !search || t.cliente.toLowerCase().includes(search.toLowerCase()) || t.asunto.toLowerCase().includes(search.toLowerCase()) || t.numero.toLowerCase().includes(search.toLowerCase());      const matchEstado = !filtroEstado || t.estado === filtroEstado;      const matchPrioridad = !filtroPrioridad || t.prioridad === filtroPrioridad;      return matchSearch && matchEstado && matchPrioridad;    });  }, [search, filtroEstado, filtroPrioridad, items]);  const totalPages = Math.ceil(filtrados.length / 8);  const paginados = useMemo(() => {    const start = (currentPage - 1) * 8;    return filtrados.slice(start, start + 8);  }, [filtrados, currentPage]);  const showToast = (message: string) => {    setToast({ visible: true, message });    setTimeout(() => setToast({ visible: false, message: '' }), 3000);  };  const abrirNuevo = () => {    setEditando(null);    setForm({ prioridad: 'media', estado: 'abierto' });    setModalOpen(true);  };  const abrirEditar = (t: Ticket) => {    setEditando(t);    setForm({ ...t });    setModalOpen(true);  };  const guardar = () => {    setSaving(true);    setTimeout(() => {      if (editando) {        setItems(prev => prev.map(p => p.id === editando.id ? { ...p, ...form } : p));      } else {        setItems(prev => [...prev, { ...form, id: crypto.randomUUID() } as Ticket]);      }      setSaving(false);      setModalOpen(false);      showToast(editando ? 'Ticket actualizado exitosamente' : 'Ticket creado exitosamente');    }, 1000);  };  const confirmarEliminar = (id: string) => {    setDeleteTarget(id);    setDeleteOpen(true);  };  const eliminar = () => {    if (!deleteTarget) return;    setItems(prev => prev.filter(p => p.id !== deleteTarget));    setDeleteTarget(null);    setDeleteOpen(false);    showToast('Ticket eliminado exitosamente');  };  const handleFormChange = (field: keyof Ticket, value: string) => {    setForm(prev => ({ ...prev, [field]: value }));  };  return (    <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }}>        <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className="mb-8">          <h1 className="text-2xl font-bold text-text-primary">Atención al Cliente</h1>          <p className="text-text-secondary mt-1">Gestión de tickets y atención al cliente</p>        </motion.div>        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-6">          {summaryCards.map((card, i) => (            <motion.div              key={card.title}              initial={{ opacity: 0, y: 20 }}              animate={{ opacity: 1, y: 0 }}              transition={{ delay: i * 0.1 }}              className="bg-white rounded-xl border border-border p-5"            >              <div className="flex items-center justify-between">                <div>                  <p className="text-sm text-text-secondary">{card.title}</p>                  <p className="text-2xl font-bold text-text-primary mt-1">{card.value}</p>                </div>                <div className={`w-12 h-12 rounded-lg bg-primary-50 flex items-center justify-center ${card.color}`}>                  <card.icon className="w-6 h-6" />                </div>              </div>            </motion.div>          ))}        </div>        <div className="bg-white rounded-xl border border-border p-5 mb-6">          <div className="flex flex-wrap items-center gap-4">            <div className="relative flex-1 min-w-[240px]">              <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-text-secondary" />              <input                type="text"                placeholder="Buscar ticket..."                value={search}                onChange={e => { setSearch(e.target.value); setCurrentPage(1); }}                className="w-full rounded-lg border border-border bg-white pl-10 pr-3 py-2.5 text-sm text-text-primary placeholder:text-text-secondary focus:outline-none focus:ring-2 focus:ring-primary-500"              />            </div>            <select              value={filtroEstado}              onChange={e => { setFiltroEstado(e.target.value); setCurrentPage(1); }}              className="rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-secondary focus:outline-none focus:ring-2 focus:ring-primary-500"            >              <option value="">Todos los estados</option>              <option value="abierto">Abierto</option>              <option value="en_proceso">En Proceso</option>              <option value="pendiente">Pendiente</option>              <option value="resuelto">Resuelto</option>              <option value="cerrado">Cerrado</option>            </select>            <select              value={filtroPrioridad}              onChange={e => { setFiltroPrioridad(e.target.value); setCurrentPage(1); }}              className="rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-secondary focus:outline-none focus:ring-2 focus:ring-primary-500"            >              <option value="">Todas las prioridades</option>              <option value="baja">Baja</option>              <option value="media">Media</option>              <option value="alta">Alta</option>              <option value="critica">Crítica</option>            </select>            <button              onClick={abrirNuevo}              className="inline-flex items-center gap-2 bg-primary-500 text-white px-4 py-2.5 rounded-lg text-sm font-medium hover:bg-primary-600 transition-colors"            >              <Plus className="w-4 h-4" />              Nuevo            </button>          </div>        </div>        <div className="bg-white rounded-xl border border-border overflow-hidden">          {filtrados.length === 0 ? (            <div className="flex flex-col items-center justify-center py-16 px-4">              <div className="w-16 h-16 rounded-full bg-primary-50 flex items-center justify-center mb-4">                <Search className="w-8 h-8 text-primary-500" />              </div>              <h3 className="text-lg font-semibold text-text-primary mb-1">No se encontraron tickets</h3>              <p className="text-sm text-text-secondary text-center max-w-sm">Intenta ajustar los filtros o términos de búsqueda</p>            </div>          ) : (            <div className="overflow-x-auto">              <table className="w-full">                <thead>                  <tr className="border-b border-border bg-gray-50">                    <th className="text-left px-4 py-3 text-xs font-semibold text-text-secondary uppercase tracking-wider">Número</th>                    <th className="text-left px-4 py-3 text-xs font-semibold text-text-secondary uppercase tracking-wider">Cliente</th>                    <th className="text-left px-4 py-3 text-xs font-semibold text-text-secondary uppercase tracking-wider">Asunto</th>                    <th className="text-center px-4 py-3 text-xs font-semibold text-text-secondary uppercase tracking-wider">Prioridad</th>                    <th className="text-center px-4 py-3 text-xs font-semibold text-text-secondary uppercase tracking-wider">Estado</th>                    <th className="text-left px-4 py-3 text-xs font-semibold text-text-secondary uppercase tracking-wider">Fecha</th>                    <th className="text-right px-4 py-3 text-xs font-semibold text-text-secondary uppercase tracking-wider">Acciones</th>                  </tr>                </thead>                <tbody className="divide-y divide-border">                  {paginados.map(t => (                    <tr key={t.id} className="hover:bg-gray-50 transition-colors">                      <td className="px-4 py-3 text-sm text-text-primary font-mono">{t.numero}</td>                      <td className="px-4 py-3 text-sm text-text-primary font-medium">{t.cliente}</td>                      <td className="px-4 py-3 text-sm text-text-secondary max-w-[200px] truncate">{t.asunto}</td>                      <td className="px-4 py-3 text-center">                        <span className={`inline-flex items-center rounded-full px-2.5 py-1 text-xs font-medium ${prioridadBadge[t.prioridad]}`}>                          {t.prioridad.charAt(0).toUpperCase() + t.prioridad.slice(1)}                        </span>                      </td>                      <td className="px-4 py-3 text-center">                        <span className={`inline-flex items-center rounded-full px-2.5 py-1 text-xs font-medium ${estadoBadge[t.estado]}`}>                          {t.estado === 'en_proceso' ? 'En Proceso' : t.estado.charAt(0).toUpperCase() + t.estado.slice(1)}                        </span>                      </td>                      <td className="px-4 py-3 text-sm text-text-secondary">{t.fecha}</td>                      <td className="px-4 py-3 text-right">                        <div className="flex items-center justify-end gap-1">                          <button className="p-2 rounded-lg text-text-secondary hover:bg-gray-100 hover:text-primary-500 transition-colors" title="Ver"><Eye className="w-4 h-4" /></button>                          <button onClick={() => abrirEditar(t)} className="p-2 rounded-lg text-text-secondary hover:bg-gray-100 hover:text-primary-500 transition-colors" title="Editar"><Edit2 className="w-4 h-4" /></button>                          <button onClick={() => confirmarEliminar(t.id)} className="p-2 rounded-lg text-text-secondary hover:bg-gray-100 hover:text-error transition-colors" title="Eliminar"><Trash2 className="w-4 h-4" /></button>                        </div>                      </td>                    </tr>                  ))}                </tbody>              </table>            </div>          )}          {filtrados.length > 0 && (            <div className="flex items-center justify-between px-4 py-3 border-t border-border">              <span className="text-sm text-text-secondary">                Mostrando {Math.min((currentPage - 1) * 8 + 1, filtrados.length)}-{Math.min(currentPage * 8, filtrados.length)} de {filtrados.length}              </span>              <div className="flex items-center gap-1">                {Array.from({ length: totalPages }, (_, i) => i + 1).map(page => (                  <button                    key={page}                    onClick={() => setCurrentPage(page)}                    className={`inline-flex items-center justify-center min-w-[36px] h-9 px-2 rounded-lg border text-sm font-medium transition-colors ${page === currentPage ? 'bg-primary-500 text-white border-primary-500' : 'border-border text-text-secondary hover:bg-gray-100'}`}                  >                    {page}                  </button>                ))}              </div>            </div>          )}        </div>      <AnimatePresence>        {modalOpen && (          <motion.div key="modal" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 z-50 flex items-center justify-center p-4">            <div className="absolute inset-0 bg-black/40 backdrop-blur-sm" onClick={() => setModalOpen(false)} />            <motion.div initial={{ opacity: 0, scale: 0.95 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0, scale: 0.95 }} className="relative w-full max-w-lg bg-white rounded-xl shadow-xl">              <div className="flex items-center justify-between px-6 pt-6 pb-0">                <h2 className="text-lg font-semibold text-text-primary">{editando ? 'Editar Ticket' : 'Nuevo Ticket'}</h2>                <button onClick={() => setModalOpen(false)} className="p-1 rounded-lg text-text-secondary hover:bg-gray-100 transition-colors"><X className="w-5 h-5" /></button>              </div>              <div className="px-6 py-6 space-y-4">                <div className="grid grid-cols-2 gap-4">                  <div>                    <label className="block text-sm font-medium text-text-primary mb-1">Número</label>                    <input type="text" value={form.numero || ''} onChange={e => handleFormChange('numero', e.target.value)} className="w-full rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-primary-500" />                  </div>                  <div>                    <label className="block text-sm font-medium text-text-primary mb-1">Cliente</label>                    <input type="text" value={form.cliente || ''} onChange={e => handleFormChange('cliente', e.target.value)} className="w-full rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-primary-500" />                  </div>                </div>                <div>                  <label className="block text-sm font-medium text-text-primary mb-1">Asunto</label>                  <input type="text" value={form.asunto || ''} onChange={e => handleFormChange('asunto', e.target.value)} className="w-full rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-primary-500" />                </div>                <div>                  <label className="block text-sm font-medium text-text-primary mb-1">Categoría</label>                  <input type="text" value={form.categoria || ''} onChange={e => handleFormChange('categoria', e.target.value)} className="w-full rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-primary-500" />                </div>                <div className="grid grid-cols-2 gap-4">                  <div>                    <label className="block text-sm font-medium text-text-primary mb-1">Prioridad</label>                    <select value={form.prioridad || 'media'} onChange={e => handleFormChange('prioridad', e.target.value)} className="w-full rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-primary-500">                      <option value="baja">Baja</option>                      <option value="media">Media</option>                      <option value="alta">Alta</option>                      <option value="critica">Crítica</option>                    </select>                  </div>                  <div>                    <label className="block text-sm font-medium text-text-primary mb-1">Estado</label>                    <select value={form.estado || 'abierto'} onChange={e => handleFormChange('estado', e.target.value)} className="w-full rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-primary-500">                      <option value="abierto">Abierto</option>                      <option value="en_proceso">En Proceso</option>                      <option value="pendiente">Pendiente</option>                      <option value="resuelto">Resuelto</option>                      <option value="cerrado">Cerrado</option>                    </select>                  </div>                </div>                <div className="grid grid-cols-2 gap-4">                  <div>                    <label className="block text-sm font-medium text-text-primary mb-1">Fecha</label>                    <input type="date" value={form.fecha || ''} onChange={e => handleFormChange('fecha', e.target.value)} className="w-full rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-primary-500" />                  </div>                  <div>                    <label className="block text-sm font-medium text-text-primary mb-1">Usuario</label>                    <input type="text" value={form.usuario || ''} onChange={e => handleFormChange('usuario', e.target.value)} className="w-full rounded-lg border border-border bg-white px-3 py-2.5 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-primary-500" />                  </div>                </div>              </div>              <div className="px-6 pb-6 flex items-center justify-end gap-3">                <button onClick={() => setModalOpen(false)} className="px-4 py-2 rounded-lg border border-border text-text-secondary text-sm font-medium hover:bg-gray-100 transition-colors">Cancelar</button>                <button onClick={guardar} disabled={saving} className="inline-flex items-center gap-2 bg-primary-500 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-primary-600 transition-colors disabled:opacity-50">                  {saving && <Loader2 className="w-4 h-4 animate-spin" />}                  {saving ? 'Guardando...' : 'Guardar'}                </button>              </div>            </motion.div>          </motion.div>        )}      </AnimatePresence>      <AnimatePresence>        {deleteOpen && (          <motion.div key="delete" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 z-50 flex items-center justify-center p-4">            <div className="absolute inset-0 bg-black/40 backdrop-blur-sm" onClick={() => setDeleteOpen(false)} />            <motion.div initial={{ opacity: 0, scale: 0.95 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0, scale: 0.95 }} className="relative w-full max-w-sm bg-white rounded-xl shadow-xl">              <div className="px-6 pt-6 pb-4 text-center">                <div className="w-12 h-12 rounded-full bg-error-lighter flex items-center justify-center mx-auto mb-4">                  <AlertTriangle className="w-6 h-6 text-error" />                </div>                <h2 className="text-lg font-semibold text-text-primary mb-2">Confirmar eliminación</h2>                <p className="text-sm text-text-secondary">¿Estás seguro de eliminar este ticket? Esta acción no se puede deshacer.</p>              </div>              <div className="px-6 pb-6 flex items-center justify-center gap-3">                <button onClick={() => setDeleteOpen(false)} className="px-4 py-2 rounded-lg border border-border text-text-secondary text-sm font-medium hover:bg-gray-100 transition-colors">Cancelar</button>                <button onClick={eliminar} className="px-4 py-2 rounded-lg bg-error text-white text-sm font-medium hover:bg-red-700 transition-colors">Eliminar</button>              </div>            </motion.div>          </motion.div>        )}      </AnimatePresence>      <AnimatePresence>        {toast.visible && (          <motion.div key="toast"            initial={{ opacity: 0, x: 100, scale: 0.95 }}            animate={{ opacity: 1, x: 0, scale: 1 }}            exit={{ opacity: 0, x: 100, scale: 0.95 }}            className="fixed bottom-6 right-6 z-50 max-w-sm w-full bg-white rounded-lg shadow-lg border border-border border-l-4 border-l-success px-4 py-3"          >            <div className="flex items-start gap-3">              <CheckCircle className="w-5 h-5 mt-0.5 flex-shrink-0 text-success" />              <p className="flex-1 text-sm text-text-primary">{toast.message}</p>              <button onClick={() => setToast({ visible: false, message: '' })} className="p-1 rounded-lg text-text-secondary hover:text-text-primary hover:bg-gray-100 transition-colors flex-shrink-0"><X className="w-4 h-4" /></button>            </div>          </motion.div>        )}      </AnimatePresence>    </motion.div>  );}export default AtencionClientePage;